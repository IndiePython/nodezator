Modules and subpackages inside the 'syntaxes' package
contain syntax mapping functions for specific syntaxes.

For instance, the "python" subpackage has a Python syntax
mapping function in its main module.
